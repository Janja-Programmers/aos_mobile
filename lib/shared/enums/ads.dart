enum PricingRequirement { hidden, optional, required }

enum AdAttributeType {
  text,
  number,
  select,
  multiselect,
  boolean,
  date,
  year,
  unknown,
}

enum DraftSource { create, draft, edit }

enum AdStatus { reviewing, active, declined }

enum AdFormMode { create, edit }
